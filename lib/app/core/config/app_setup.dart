class AppSetup {
  final bool isDioLogger;
  final bool isDebugPrint;
  final bool isDebugPayment;

  const AppSetup({
    required this.isDioLogger,
    required this.isDebugPrint,
    required this.isDebugPayment,
  });
}
